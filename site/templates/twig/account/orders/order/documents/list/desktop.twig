{% import "util/macros/html.twig" as html %}

<div class="list-group">
	<div class="list-group-item {{ browseragent.isMobile() ? 'px-2' : '' }} bg-light text-bold">
		{% block header_display %}
			<div class="row">
				<div class="col-3">Folder</div>
				<div class="col-2">Title</div>
				<div class="col-2">Date</div>
				<div class="col-2">Time</div>
			</div>
		{% endblock %}
	</div>
	{% if documents.count() == 0 %}
		<div class="list-group-item {{ browseragent.isMobile() ? 'px-2' : '' }} text-bold">
			No Documents Found
		</div>
	{% endif %}
	{% for document in documents %}
		<div class="list-group-item {{ browseragent.isMobile() ? 'px-2' : '' }}">
			{% block item_display %}
				<div class="row">
					<div class="col-3">{{ document.directory.description }}</div>
					<div class="col-2">{{ document.title }}</div>		
					<div class="col-2">{{ document.date|convertdate }}</div>
					<div class="col-2">{{ (document.date ~ document.time[:4])|convertdate("h:i A") }}</div>
					<div class="col-2">
						<a href="{{ page.downloadDocumentUrl(page.ordn, document.folder, document.filename) }}" class="btn btn-sm {{ CSS.colors.buttons.getClass('link') }}" target="_blank">
							{{ html.iconType({'type': 'document'}) }} Download
						</a>
					</div>
				</div>
			{% endblock %}
		</div>
	{% endfor %}
</div>