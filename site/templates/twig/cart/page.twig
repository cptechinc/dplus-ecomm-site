{% import "util/macros/html.twig" as html %}

<section class="bar pt-4">
	<div class="heading">
		<h2>{{ page.title }}</h2>
	</div>
	
	<div class="mb-2">
		{% include 'cart/items.twig' %}
	</div>

	<a href="#" class="btn btn-block {{ CSS.colors.buttons.getClass('submit') }} text-white">
		{{ html.iconType({'type': 'cart'}) }} Checkout
	</a>
</section>
